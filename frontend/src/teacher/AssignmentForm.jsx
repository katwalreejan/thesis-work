export default function AssignmentForm({ form, updateField, saving, onSave, onCancel }) {
  return <form className="assignment-form" onSubmit={onSave}><label>Assignment name<input required value={form.filename} onChange={updateField('filename')} /></label><label>Extracted work<textarea value={form.text} onChange={updateField('text')} rows="3" /></label><label>Feedback<textarea value={form.feedback} onChange={updateField('feedback')} rows="3" /></label><label>Result<select value={form.is_correct} onChange={updateField('is_correct')}><option value="">Not graded</option><option value="true">Correct</option><option value="false">Needs work</option></select></label><div className="assignment-form-actions"><button type="button" onClick={onCancel}>Cancel</button><button className="primary-button" disabled={saving} type="submit">{saving ? 'Saving...' : 'Save assignment'}</button></div></form>
}
